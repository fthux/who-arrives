{
  "name": "who-arrives",
  "version": "1.0.0",
  "description": "Client IP, geolocation, and network information from Cloudflare Workers only.",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "data:build": "node scripts/build-country-data.mjs",
    "docs:build": "node scripts/build-openapi.mjs",
    "test:dev": "node scripts/verify-dev.mjs",
    "types": "wrangler types",
    "typecheck": "npm run types && tsc --noEmit",
    "test": "tsx --test test/*.test.ts",
    "check": "npm run typecheck && npm test && wrangler deploy --dry-run",
    "deploy": "wrangler deploy"
  },
  "keywords": [],
  "author": "",
  "license": "AGPL-3.0-only",
  "type": "module",
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260915.1",
    "tsx": "^4.23.13",
    "typescript": "^7.0.2",
    "wrangler": "^4.131.2"
  }
}
